
describe("Authenticate User Controller", () => {
  it("should be able to authenticate an user", async () => {
    const user = {
      name: "User Example",
      email: "user@memail.com",
      password: "1234",
    }
  });
});
